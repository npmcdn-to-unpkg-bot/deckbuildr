import * as actionTypes from '../actions/actionTypes';

/**
 * categoryIds reducer that keeps track of the all the categories in the system by ID.
 * @param state
 * @param action
 * @returns {Array}
 */
export default function categoryIds (state = [], action) {
  switch (action.type) {
    default: {
      return state;
    }
  }
}
